# Guides

How to _use_ the library, aimed at someone who has never seen it.

The first one to write is getting started: zero to a component on screen in two
minutes. It is also the acceptance test for the package — if the guide alone is
not enough to install the library into an empty project, the library is not
ready to publish.
