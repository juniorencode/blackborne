{
  // Library source only, and deliberately WITHOUT Node types: the package
  // runs in a browser, and a stray `process.env` should not typecheck.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "types": ["vite/client"]
  },
  "include": ["src"]
}
